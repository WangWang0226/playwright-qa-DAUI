# Playwright Test Cases SPEC

## Metadata

- Source document: `playwright-tests/qa-source-test-cases.md`
- Source repo: Digital Asset product repos. Current UI-aware refinement source is OpsUI / Ops-UI.
- Generated at: 2026-09-15
- Generated by: `abd-ops-ui-testcase-refiner`
- Product/app: Digital Asset platform. Current UI target is OpsUI / DASH Ops UI; current API target is Digital Asset Orchestrator.
- Environment assumptions:
  - React + Vite app using `react-router-dom`.
  - Authenticated routes are protected by `PrivateLayout`.
  - Login route is `/login`.
  - Primary authenticated routes are `/dashboard`, `/transactions/new`, `/transactions/history`, `/queue`, `/liquidity`, and `/accounts/:accountId`.
  - Current source does not define a dedicated wallet-management route, operational-health route, explicit transaction-type selector, or explicit blockchain-network selector. Any tests requiring those controls are marked `blocked` or `needs-live-verification`.
  - Live deployment may differ from this source. Locators below are source-derived and should be verified against live DOM before implementation.

## Test Case Index

| TC ID | Source Case | Title | Type | Role | Route | Mutation Risk | Automation Readiness |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TC-UI-OPS-AUTH-001 | OPS-AUTH-001 | Open the Ops UI after successful login | smoke | admin / operations user | `/login`, `/dashboard` | none | ready |
| TC-UI-OPS-AUTH-002 | OPS-AUTH-002 | Block a user without transaction entitlement | functional | no-entitlement user | `/transactions/new` | none | needs-live-verification |
| TC-UI-OPS-AUTH-003 | OPS-AUTH-003 | Preserve read-only access | functional | read-only user | `/transactions/history` | none | needs-live-verification |
| TC-UI-OPS-TXN-001 | OPS-TXN-001 | Display accounts and wallets for transaction initiation | functional | operations user | `/transactions/new` | none | ready |
| TC-UI-OPS-TXN-002 | OPS-TXN-002 | Reject incomplete transaction input | functional | operations user | `/transactions/new` | none | ready |
| TC-UI-OPS-TXN-003 | OPS-TXN-003 | Confirm valid mint details before submission | functional | operations user | `/transactions/new` | none | needs-live-verification |
| TC-UI-OPS-TXN-004 | OPS-TXN-004 | Submit a valid mint request | workflow | operations user | `/transactions/new`, `/queue`, `/transactions/history` | creates backend/UI transaction | needs-live-verification |
| TC-UI-OPS-TXN-005 | OPS-TXN-005 | Keep a mint request pending approval | workflow | operations user / approver | `/transactions/new`, `/queue` | creates backend/UI transaction | ready |
| TC-UI-OPS-TXN-006 | OPS-TXN-006 | Approve a pending mint request | workflow | approver/admin | `/queue`, `/transactions/history` | approves backend/UI transaction | ready |
| TC-UI-OPS-TXN-007 | OPS-TXN-007 | Reject a mint when fiat reserve is unavailable | functional | operations user | `/transactions/new`, `/queue`, `/transactions/history` | creates failed transaction | blocked |
| TC-UI-OPS-TXN-008 | OPS-TXN-008 | Display a Fireblocks mint failure | functional | operations user | `/transactions/history` | creates failed transaction | blocked |
| TC-UI-OPS-TXN-009 | OPS-TXN-009 | Avoid duplicate mint submission | workflow | operations user | `/transactions/new`, `/queue`, `/transactions/history` | creates transaction | needs-live-verification |
| TC-UI-OPS-LIFE-001 | OPS-LIFE-001 | Submit a wallet-to-wallet transfer | workflow | operations user | `/transactions/new`, `/queue`, `/transactions/history` | creates backend/UI transaction | needs-live-verification |
| TC-UI-OPS-LIFE-002 | OPS-LIFE-002 | Prevent transfer from an ineligible wallet | functional | operations user | `/transactions/new` | none | blocked |
| TC-UI-OPS-LIFE-003 | OPS-LIFE-003 | Submit a burn transaction | workflow | operations user | `/transactions/new`, `/queue`, `/transactions/history` | creates backend/UI transaction | needs-live-verification |
| TC-UI-OPS-WAL-001 | OPS-WAL-001 | Submit a new wallet request | workflow | wallet-management user | TBD | creates wallet request | blocked |
| TC-UI-OPS-WAL-002 | OPS-WAL-002 | Display wallet creation status | functional | wallet-management user | TBD | none | blocked |
| TC-UI-OPS-WAL-003 | OPS-WAL-003 | Submit an existing wallet modification | workflow | wallet-management user | TBD | updates wallet | blocked |
| TC-UI-OPS-HIST-001 | OPS-HIST-001 | View transaction status by reference | functional | operations user | `/transactions/history` | none | needs-live-verification |
| TC-UI-OPS-HIST-002 | OPS-HIST-002 | Filter transaction history | functional | operations user | `/transactions/history` | none | ready |
| TC-UI-OPS-HIST-003 | OPS-HIST-003 | Display an unavailable transaction outcome | functional | operations user | `/transactions/history` | none | blocked |
| TC-UI-OPS-HIST-004 | OPS-HIST-004 | Download filtered transactions | functional | operations user | `/transactions/history` | downloads file | needs-live-verification |
| TC-UI-OPS-OBS-001 | OPS-OBS-001 | Display service health or unavailable state | functional | operations user | TBD | none | blocked |
| TC-UI-OPS-OBS-002 | OPS-OBS-002 | Recover a processing transaction after delayed webhook | workflow | operations user | `/transactions/history` | creates/updates transaction | needs-live-verification |

## Source-Derived UI Contract

Routes:
- `/login`: login page.
- `/dashboard`: authenticated landing page.
- `/transactions/new`: transaction initiation form.
- `/queue`: approval queue.
- `/transactions/history`: transaction history.
- `/liquidity`: admin-visible liquidity management.
- `/accounts/:accountId`: account details.

Known visible navigation:
- `Dashboard`
- `New Transaction`
- `Queue`
- `History`
- `Liquidity`
- `Logout`

Known login controls:
- Username input placeholder: `Username or Email`
- Password input placeholder: `Password`
- Submit button text: `Sign In`
- Simulation panel may appear when backend is offline.

Known transaction form controls:
- Page heading in source: `Initiate Transfer`
- Panel heading: `Global Clearing Entry`
- Source dropdown label: `Source Account`
- Destination dropdown label: `Destination Account`
- Amount input placeholder: `0.00`
- Memo input placeholder: `e.g. Treasury Rebalancing`
- Submit button text when valid: `Confirm Transaction Initiation`
- Disabled submit labels include `Accounts Must Differ`, `Insufficient Funds`, `Fiat-to-Fiat Transfers Disabled`, and `On-Chain Wallets Must Differ`.

Known approval queue controls:
- Page heading: `Approval Queue`
- Table title: `Pending Verification`
- Actions: `Approve`, `Reject`
- Initiator shown under `Requested By`.

Known history controls:
- Page heading: `Transaction Log`
- Table title: `Transaction History`
- Export button: `Export Data`
- Filter placeholders: `Filter Sender...`, `Filter Receiver...`, `Min`, `Max`
- Status dropdown values derive from `TransactionStatus`: `PENDING APPROVAL`, `APPROVED`, `REJECTED`, `COMPLETED`.

Global selector gap:
- No `data-testid` values were found in the current UI source. Most recommended locators are based on role, visible text, placeholders, or table text and should be hardened.

## Test Cases

### TC-UI-OPS-AUTH-001 - Open the Ops UI after successful login

Source requirement:
- `OPS-AUTH-001 — Open the Ops UI after successful login`

Type:
- smoke

Role:
- Admin or operations user with dashboard access.
- Source mock users include `emily@scotiabank.com` as global admin and `alice@scotiabank.com` / `charlie@scotiabank.com` as approvers.
- Live seeded user should be provided by environment/configuration.

Routes:
- `/login`
- `/dashboard`

Preconditions:
- User can authenticate through backend or simulation login.
- Authenticated user has at least dashboard access.

Test Data:
- username: environment-provided admin or operations username.
- password: environment-provided password.

UI Operation Steps:
1. Navigate to `/login`.
2. Fill `Username or Email`.
3. Fill `Password`.
4. Click `Sign In`.
5. Wait for redirect away from `/login`.
6. Verify landing page/dashboard content and authenticated sidebar.

Expected Assertions:
- URL is `/dashboard` or authenticated landing equivalent.
- Sidebar/nav contains `Dashboard`.
- Authenticated session panel shows current user name/email.
- Login submit controls are no longer the active page controls.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Username input | `getByPlaceholder('Username or Email')` | acceptable | `Login.tsx` |
| Password input | `getByPlaceholder('Password')` | acceptable | `Login.tsx` |
| Sign In button | `getByRole('button', { name: 'Sign In' })` | stable | `Login.tsx` |
| Dashboard nav | `getByRole('link', { name: 'Dashboard' })` | stable | `Sidebar.tsx` |
| Session panel | `getByText(/Session/i)` | acceptable | `Sidebar.tsx` |

Page Object Suggestions:
- `LoginPage.goto()`
- `LoginPage.login(username, password)`
- `DashboardPage.expectLoaded()`

Async/Wait Rules:
- Wait for URL not ending in `/login`.
- Wait for `Dashboard` nav/sidebar to render.

Mutation Risk:
- none

Automation Readiness:
- ready

Data-testid Recommendations:
- `login-username`
- `login-password`
- `login-submit`
- `sidebar-session-user`

Open Questions:
- Confirm canonical non-admin operations user for integrated environment.

### TC-UI-OPS-AUTH-002 - Block a user without transaction entitlement

Source requirement:
- `OPS-AUTH-002 — Block a user without transaction entitlement`

Type:
- functional

Role:
- No transaction entitlement user. Raw case uses `Drew Martin`.

Routes:
- `/transactions/new`
- fallback route may be `/dashboard` or `/login`.

Preconditions:
- A seeded user exists with no operate permission and no transaction-initiation entitlement.
- Current source only provides mock viewer `frank@scotiabank.com`, but viewer can still see history; exact no-entitlement user is not source-confirmed.

Test Data:
- username: no-entitlement fixture to be confirmed.

UI Operation Steps:
1. Authenticate as no-entitlement user.
2. Navigate directly to `/transactions/new`.
3. Observe final route and page state.

Expected Assertions:
- User is redirected or shown protected/access-denied state.
- `Confirm Transaction Initiation` is not visible/enabled.
- `Source Account`, `Destination Account`, and amount controls are not available for submission.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| New transaction route | direct navigation to `/transactions/new` | stable | `App.tsx` |
| Submit button absence | `getByRole('button', { name: 'Confirm Transaction Initiation' })` should be hidden/absent | acceptable | `TransactionForm.tsx` |
| Dashboard fallback | `getByRole('link', { name: 'Dashboard' })` | stable | `Sidebar.tsx` |

Page Object Suggestions:
- `LoginPage.loginAsNoEntitlement(...)`
- `TransactionPage.expectAccessDeniedOrRedirected()`

Async/Wait Rules:
- Wait for route settlement after direct navigation.

Mutation Risk:
- none

Automation Readiness:
- needs-live-verification

Data-testid Recommendations:
- `access-denied-message`
- `transaction-form`
- `transaction-submit`

Open Questions:
- Which seeded user has no DASH transaction entitlement?
- Should final state be redirect or explicit access-denied message?

### TC-UI-OPS-AUTH-003 - Preserve read-only access

Source requirement:
- `OPS-AUTH-003 — Preserve read-only access`

Type:
- functional

Role:
- Read-only user. Source mock user: `frank@scotiabank.com` viewer.

Routes:
- `/transactions/history`
- `/transactions/new`
- `/queue`

Preconditions:
- Read-only user can authenticate.
- Viewer has `can_view` and no `can_operate`.

Test Data:
- username: `frank@scotiabank.com` or live read-only user.

UI Operation Steps:
1. Authenticate as read-only user.
2. Navigate to `/transactions/history`.
3. Verify transaction history page and filters.
4. Inspect sidebar actions.
5. Attempt direct navigation to `/transactions/new` and `/queue`.

Expected Assertions:
- History rows or empty history state are visible.
- `New Transaction` and `Queue` nav links are hidden when `hasOperate` is false.
- `Confirm Transaction Initiation`, `Approve`, and `Reject` actions are not available.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| History nav | `getByRole('link', { name: 'History' })` | stable | `Sidebar.tsx` |
| History heading | `getByRole('heading', { name: 'Transaction Log' })` | stable | `TransactionHistory.tsx` |
| New Transaction nav absence | `getByRole('link', { name: 'New Transaction' })` hidden | stable | `Sidebar.tsx` |
| Queue nav absence | `getByRole('link', { name: 'Queue' })` hidden | stable | `Sidebar.tsx` |

Page Object Suggestions:
- `HistoryPage.expectReadOnlyVisible()`
- `Sidebar.expectNoOperateNavigation()`

Async/Wait Rules:
- Wait for auth storage and sidebar render.

Mutation Risk:
- none

Automation Readiness:
- needs-live-verification

Data-testid Recommendations:
- `nav-new-transaction`
- `nav-queue`
- `history-table`

Open Questions:
- Confirm live read-only user and expected direct-route behavior.

### TC-UI-OPS-TXN-001 - Display accounts and wallets for transaction initiation

Source requirement:
- `OPS-TXN-001 — Display accounts and wallets for transaction initiation`

Type:
- functional

Role:
- Operations user with operate permission.

Routes:
- `/transactions/new`

Preconditions:
- User has at least one operable account.
- Accounts are loaded into app context/localStorage before opening the form.

Test Data:
- source account: source-derived mock examples include `MOCK - US Branch Account`, `MOCK - Canadian Branch Account`.
- destination account: source-derived mock examples include `MOCK - U.S. Branch Wallet`, `MOCK - Canadian Branch Wallet`.
- live examples may differ.

UI Operation Steps:
1. Authenticate as operations/admin user.
2. Navigate to `/transactions/new`.
3. Verify `Global Clearing Entry` form.
4. Open `Source Account` dropdown.
5. Verify operable fiat/digital accounts are shown with account name, ID, balance or `Restricted Balance`, and permission badges.
6. Select a source account.
7. Open `Destination Account` dropdown.
8. Verify destination accounts are shown, excluding selected source.

Expected Assertions:
- Heading `Initiate Transfer` is visible.
- `Source Account`, `Destination Account`, `Transfer Amount`, and `Reference Memo` controls are visible.
- Account dropdown options show account names and `#<account id>`.
- Balance/reserve display is visible unless permission-restricted.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Page heading | `getByRole('heading', { name: 'Initiate Transfer' })` | stable | `TransactionForm.tsx` |
| Source dropdown | `getByText('Source Account').locator(...).getByRole('button')` | fragile | `TransactionForm.tsx` |
| Destination dropdown | `getByText('Destination Account').locator(...).getByRole('button')` | fragile | `TransactionForm.tsx` |
| Amount input | `getByPlaceholder('0.00')` | acceptable | `TransactionForm.tsx` |
| Memo input | `getByPlaceholder('e.g. Treasury Rebalancing')` | acceptable | `TransactionForm.tsx` |

Page Object Suggestions:
- `TransactionFormPage.goto()`
- `TransactionFormPage.openSourceAccountDropdown()`
- `TransactionFormPage.selectSourceAccount(name)`
- `TransactionFormPage.selectDestinationAccount(name)`

Async/Wait Rules:
- Wait until accounts have loaded and dropdown options are visible.

Mutation Risk:
- none

Automation Readiness:
- ready

Data-testid Recommendations:
- `transaction-form`
- `transaction-source-account-dropdown`
- `transaction-source-account-option`
- `transaction-destination-account-dropdown`
- `transaction-destination-account-option`

Open Questions:
- Confirm canonical account names in integrated QA environment.

### TC-UI-OPS-TXN-002 - Reject incomplete transaction input

Source requirement:
- `OPS-TXN-002 — Reject incomplete transaction input`

Type:
- functional

Role:
- Operations user.

Routes:
- `/transactions/new`

Preconditions:
- User can open transaction form.

Test Data:
- none

UI Operation Steps:
1. Navigate to `/transactions/new`.
2. Leave amount empty or `0`.
3. Leave memo empty.
4. If source/destination can be cleared, clear one account selection.
5. Observe submit button state.
6. Attempt form submit only if browser allows it.

Expected Assertions:
- Submit button is disabled or native required validation prevents submission.
- Button text is not `Confirm Transaction Initiation` when required account state is invalid.
- No success banner appears.
- URL does not navigate to `/queue`.
- No transaction reference is shown.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Amount input | `getByPlaceholder('0.00')` | acceptable | `TransactionForm.tsx` |
| Memo input | `getByPlaceholder('e.g. Treasury Rebalancing')` | acceptable | `TransactionForm.tsx` |
| Submit button | `getByRole('button', { name: /Confirm Transaction Initiation|Insufficient Funds|Accounts Must Differ|Fiat-to-Fiat Transfers Disabled/ })` | acceptable | `TransactionForm.tsx` |

Page Object Suggestions:
- `TransactionFormPage.expectSubmitDisabled()`
- `TransactionFormPage.fillAmount(value)`

Async/Wait Rules:
- No backend wait expected.

Mutation Risk:
- none

Automation Readiness:
- ready

Data-testid Recommendations:
- `transaction-amount-input`
- `transaction-memo-input`
- `transaction-submit-button`
- `transaction-validation-message`

Open Questions:
- Current source uses native `required` fields plus disabled submit; confirm whether QA should assert native validation bubbles.

### TC-UI-OPS-TXN-003 - Confirm valid mint details before submission

Source requirement:
- `OPS-TXN-003 — Confirm valid mint details before submission`

Type:
- functional

Role:
- Operations user.

Routes:
- `/transactions/new`

Preconditions:
- Source fiat account and destination digital wallet exist.
- User has operate permission for source account.

Test Data:
- source account: fiat account.
- destination account: digital asset wallet.
- amount: `1000`.
- memo: unique QA memo.

UI Operation Steps:
1. Navigate to `/transactions/new`.
2. Select fiat source account.
3. Select digital destination wallet.
4. Fill amount.
5. Fill memo.
6. Observe form before clicking `Confirm Transaction Initiation`.

Expected Assertions:
- Selected source account name and ID are visible in `Source Account`.
- Selected destination wallet name and ID are visible in `Destination Account`.
- Amount input contains entered amount.
- Memo input contains entered memo.
- `Confirm Transaction Initiation` is enabled.
- No transaction reference exists before clicking submit.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Source dropdown selected value | dropdown button containing selected source name | fragile | `TransactionForm.tsx` |
| Destination dropdown selected value | dropdown button containing selected destination name | fragile | `TransactionForm.tsx` |
| Submit button | `getByRole('button', { name: 'Confirm Transaction Initiation' })` | stable | `TransactionForm.tsx` |

Page Object Suggestions:
- `TransactionFormPage.fillValidFiatToDigitalTransfer(data)`
- `TransactionFormPage.expectReviewReady(data)`

Async/Wait Rules:
- Wait for dropdown selection to close and selected values to render.

Mutation Risk:
- none

Automation Readiness:
- needs-live-verification

Data-testid Recommendations:
- `transaction-source-selected-account`
- `transaction-destination-selected-account`
- `transaction-submit-button`

Open Questions:
- Raw case expects a separate confirmation/review state with confirm/cancel before submission. Current source has no separate review page/modal; `Confirm Transaction Initiation` directly submits.

### TC-UI-OPS-TXN-004 - Submit a valid mint request

Source requirement:
- `OPS-TXN-004 — Submit a valid mint request`

Type:
- workflow

Role:
- Operations user with operate permission.

Routes:
- `/transactions/new`
- `/queue`
- `/transactions/history`

Preconditions:
- Source fiat account and destination digital wallet exist.
- User has operate permission.
- Backend or simulation can create a pending transaction.

Test Data:
- source account: fiat account.
- destination account: digital wallet.
- amount: QA-safe amount.
- memo: unique string.

UI Operation Steps:
1. Navigate to `/transactions/new`.
2. Select fiat source account.
3. Select digital destination wallet.
4. Fill amount.
5. Fill memo.
6. Click `Confirm Transaction Initiation`.
7. Wait for `/queue`.
8. Capture success banner transaction reference.
9. Verify the new queue row.
10. If approver flow is in scope, approve and poll history for completion.

Expected Assertions:
- Success banner contains `Transaction request <id> submitted successfully!`.
- Queue row contains transaction id, source, destination, amount, and requested-by user.
- History eventually contains matching completed/approved row after approval, if approval is included.
- Destination balance check is only assertable if UI exposes refreshed balance.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Submit button | `getByRole('button', { name: 'Confirm Transaction Initiation' })` | stable | `TransactionForm.tsx` |
| Success banner | `getByText(/Transaction request .* submitted successfully/i)` | acceptable | `ApprovalQueue.tsx` |
| Queue heading | `getByRole('heading', { name: 'Approval Queue' })` | stable | `ApprovalQueue.tsx` |
| Pending row | row filtered by transaction id / amount / account names | acceptable | `ApprovalQueue.tsx` |

Page Object Suggestions:
- `TransactionFormPage.submitTransaction(data)`
- `ApprovalQueuePage.expectSubmittedTransaction(data)`
- `HistoryPage.waitForCompletedTransaction(data)`

Async/Wait Rules:
- Wait for navigation to `/queue`.
- For completion, snapshot existing history IDs before submit; after approval poll for a new matching completed row.

Mutation Risk:
- creates backend/UI transaction

Automation Readiness:
- needs-live-verification

Data-testid Recommendations:
- `transaction-success-banner`
- `approval-queue-table`
- `approval-queue-row`
- `history-transaction-row`

Open Questions:
- Source has no explicit transaction type selector; mint is inferred from fiat-to-digital account type.
- Source does not expose blockchain network selection; live verification needed if network is required.

### TC-UI-OPS-TXN-005 - Keep a mint request pending approval

Source requirement:
- `OPS-TXN-005 — Keep a mint request pending approval`

Type:
- workflow

Role:
- Initiator operations user and approver/admin.

Routes:
- `/transactions/new`
- `/queue`

Preconditions:
- Initiator can create fiat-to-digital transaction.
- Approval queue is visible to the initiator/admin/approver.

Test Data:
- source fiat account.
- destination digital wallet.
- amount: QA-safe amount.
- memo: unique string.

UI Operation Steps:
1. Submit a fiat-to-digital transaction from `/transactions/new`.
2. Wait for `/queue`.
3. Locate the new row in `Pending Verification`.
4. Verify the initiator view shows pending state rather than completed state.

Expected Assertions:
- Queue row exists under `Pending Verification`.
- Row shows source, destination, amount, and requested-by user.
- For same initiator, action area shows `Pending Approval` / waiting message instead of active `Approve` button when peer-check blocks same-user approval.
- Destination wallet is not asserted as funded before approval.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Pending count | `getByText(/\\d+ PENDING/)` | acceptable | `ApprovalQueue.tsx` |
| Pending approval label | `getByText('Pending Approval')` | stable | `ApprovalQueue.tsx` |
| Approve button | `getByRole('button', { name: 'Approve' })` | stable | `ApprovalQueue.tsx` |

Page Object Suggestions:
- `ApprovalQueuePage.findPendingTransaction(criteria)`
- `ApprovalQueuePage.expectPendingApprovalForInitiator(criteria)`

Async/Wait Rules:
- Wait for queue row by transaction reference or unique memo-derived criteria when available.

Mutation Risk:
- creates backend/UI transaction

Automation Readiness:
- ready

Data-testid Recommendations:
- `approval-queue-pending-count`
- `approval-queue-row`
- `approval-queue-row-status`

Open Questions:
- Confirm whether same user can approve in admin mode or must use distinct approver in target environment.

### TC-UI-OPS-TXN-006 - Approve a pending mint request

Source requirement:
- `OPS-TXN-006 — Approve a pending mint request`

Type:
- workflow

Role:
- Approver/admin with valid peer-check permission.

Routes:
- `/queue`
- `/transactions/history`

Preconditions:
- A pending transaction exists in the queue.
- Approver has permission to approve the source account.

Test Data:
- pending transaction reference.

UI Operation Steps:
1. Navigate to `/queue`.
2. Locate pending row by transaction reference/source/destination/amount.
3. Click `Approve`.
4. Verify row leaves pending state or status changes.
5. Navigate to `/transactions/history`.
6. Search/filter or poll until approved/completed transaction is visible.

Expected Assertions:
- `Approve` button is enabled for valid approver.
- After approval, the request no longer appears as pending.
- History row remains locatable by reference or resulting backend id.
- Status is `APPROVED`, processing, or eventually `COMPLETED` depending environment.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Approve button | row-scoped `getByRole('button', { name: 'Approve' })` | stable | `ApprovalQueue.tsx` |
| History nav | `getByRole('link', { name: 'History' })` | stable | `Sidebar.tsx` |
| Status badge | row-scoped text matching `APPROVED|COMPLETED|PENDING APPROVAL` | acceptable | `TransactionHistory.tsx` |

Page Object Suggestions:
- `ApprovalQueuePage.approveTransaction(criteria)`
- `HistoryPage.waitForTransactionStatus(criteria, status)`

Async/Wait Rules:
- Use `expect.poll` for post-approval processing.
- Do not match old completed rows; snapshot existing history IDs before creating/approving a transaction.

Mutation Risk:
- approves backend/UI transaction

Automation Readiness:
- ready

Data-testid Recommendations:
- `approval-approve-button`
- `history-status-badge`

Open Questions:
- Confirm final status expectation for integrated flow: `APPROVED` plus processing badge or top-level `COMPLETED`.

### TC-UI-OPS-TXN-007 - Reject a mint when the fiat reserve is unavailable

Source requirement:
- `OPS-TXN-007 — Reject a mint when the fiat reserve is unavailable`

Type:
- functional

Role:
- Operations user.

Routes:
- `/transactions/new`
- `/transactions/history`

Preconditions:
- Service stub or backend fixture can force reserve rejection.

Test Data:
- amount above available reserve, e.g. `250000.01` from raw case.

UI Operation Steps:
1. Open transaction form.
2. Select source account.
3. Select destination wallet.
4. Fill amount greater than displayed balance/reserve.
5. Observe validation before submission.
6. If backend failure fixture exists, submit and observe failed status.

Expected Assertions:
- Current source supports client validation: `Insufficient Funds` and `Insufficient liquidity. Available: ...`.
- No success banner should appear.
- No successful mint should appear in history.
- Backend reserve-specific error cannot be confirmed from current source.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Insufficient funds button | `getByRole('button', { name: 'Insufficient Funds' })` | stable | `TransactionForm.tsx` |
| Liquidity message | `getByText(/Insufficient liquidity/i)` | stable | `TransactionForm.tsx` |

Page Object Suggestions:
- `TransactionFormPage.expectInsufficientLiquidity(amount)`

Async/Wait Rules:
- No backend wait if client-side over-balance validation blocks submit.

Mutation Risk:
- creates failed transaction only if backend failure path is used

Automation Readiness:
- blocked

Data-testid Recommendations:
- `transaction-insufficient-liquidity-message`

Open Questions:
- How can QA configure `mint-reserve-rejected` in UI or backend?
- Should this test assert client-side over-balance only, or service-level reserve rejection?

### TC-UI-OPS-TXN-008 - Display a Fireblocks mint failure

Source requirement:
- `OPS-TXN-008 — Display a Fireblocks mint failure`

Type:
- functional

Role:
- Operations user.

Routes:
- `/transactions/history`

Preconditions:
- Backend/stub can produce `ProcessingStatus.FAILED` with `processingError`.

Test Data:
- failed transaction reference.

UI Operation Steps:
1. Ensure a transaction exists with processing failure.
2. Open `/transactions/history`.
3. Locate failed transaction row.
4. Open transaction detail modal.

Expected Assertions:
- History row top-level status is `REJECTED` or processing badge is `Failed`.
- Modal processing pipeline displays `Processing Failed`.
- Modal displays `processingError` reason when provided.
- Failed operation remains locatable.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| History row | row filtered by transaction reference | acceptable | `TransactionHistory.tsx` |
| Failed badge | `getByText(/FAILED|Failed/)` | acceptable | `TransactionHistory.tsx`, `TransactionModal.tsx` |
| Processing failed message | `getByText('Processing Failed')` | stable | `TransactionModal.tsx` |

Page Object Suggestions:
- `HistoryPage.openTransaction(reference)`
- `TransactionModal.expectProcessingFailure(reason?)`

Async/Wait Rules:
- Poll until failed processing status appears.

Mutation Risk:
- creates failed transaction

Automation Readiness:
- blocked

Data-testid Recommendations:
- `transaction-modal`
- `transaction-processing-error`

Open Questions:
- How can QA force Fireblocks failure?
- What exact operator-readable failure reason should appear?

### TC-UI-OPS-TXN-009 - Avoid duplicate mint submission

Source requirement:
- `OPS-TXN-009 — Avoid duplicate mint submission`

Type:
- workflow

Role:
- Operations user.

Routes:
- `/transactions/new`
- `/queue`
- `/transactions/history`

Preconditions:
- User can submit a valid fiat-to-digital transaction.
- Duplicate handling behavior is defined by backend or UI.

Test Data:
- unique memo and amount.

UI Operation Steps:
1. Submit a valid transaction.
2. Capture resulting transaction reference or queue row.
3. Reload or attempt to submit same values again.
4. Observe whether UI blocks duplicate or backend returns existing reference.
5. Check queue/history for duplicates.

Expected Assertions:
- Only one transaction reference or one completed balance-impacting row should exist for the same unique submission.
- UI should not show two successful mint operations for a single submission intent.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Success banner | `getByText(/Transaction request .* submitted successfully/i)` | acceptable | `ApprovalQueue.tsx` |
| History rows | rows filtered by memo/reference/source/destination/amount | fragile | `TransactionHistory.tsx` |

Page Object Suggestions:
- `TransactionFormPage.submitTransaction(data)`
- `HistoryPage.countMatchingTransactions(criteria)`

Async/Wait Rules:
- Snapshot history rows before submission.
- Poll for new row; verify count does not exceed expected.

Mutation Risk:
- creates transaction

Automation Readiness:
- needs-live-verification

Data-testid Recommendations:
- `transaction-reference`
- `history-transaction-row`

Open Questions:
- Does UI or backend expose idempotency key / duplicate message?
- Is memo shown in history row or only in modal/backend?

### TC-UI-OPS-LIFE-001 - Submit a wallet-to-wallet transfer

Source requirement:
- `OPS-LIFE-001 — Submit a wallet-to-wallet transfer`

Type:
- workflow

Role:
- Operations user.

Routes:
- `/transactions/new`
- `/queue`
- `/transactions/history`

Preconditions:
- Source digital wallet and destination digital wallet exist.
- Source wallet has sufficient balance.

Test Data:
- source wallet: digital asset account.
- destination wallet: different digital asset account.
- amount: QA-safe amount.

UI Operation Steps:
1. Open `/transactions/new`.
2. Select digital source wallet.
3. Select different digital destination wallet.
4. Fill amount and memo.
5. Submit.
6. Verify pending queue row.
7. Approve if workflow includes completion.
8. Poll history for completed transfer.

Expected Assertions:
- Queue/history show source wallet, destination wallet, amount, and status.
- Source/destination balance deltas are asserted only if UI exposes stable refreshed balances.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Source dropdown | source account dropdown button | fragile | `TransactionForm.tsx` |
| Destination dropdown | destination account dropdown button | fragile | `TransactionForm.tsx` |
| Submit button | `getByRole('button', { name: 'Confirm Transaction Initiation' })` | stable | `TransactionForm.tsx` |

Page Object Suggestions:
- `TransactionFormPage.submitDigitalToDigitalTransfer(data)`
- `HistoryPage.waitForCompletedTransaction(data)`

Async/Wait Rules:
- Poll history for completion after approval.

Mutation Risk:
- creates backend/UI transaction

Automation Readiness:
- needs-live-verification

Data-testid Recommendations:
- `transaction-source-account-dropdown`
- `transaction-destination-account-dropdown`
- `transaction-submit-button`

Open Questions:
- Confirm canonical live wallet names and whether digital-to-digital transfer maps to backend transfer or requires network selector.

### TC-UI-OPS-LIFE-002 - Prevent transfer from an ineligible wallet

Source requirement:
- `OPS-LIFE-002 — Prevent transfer from an ineligible wallet`

Type:
- functional

Role:
- Operations user.

Routes:
- `/transactions/new`

Preconditions:
- Archived/ineligible wallet fixture exists.

Test Data:
- ineligible wallet: `WAL-ARCH-001` from raw case.

UI Operation Steps:
1. Open transaction form.
2. Open source wallet dropdown.
3. Search/select archived wallet if shown.
4. Observe disabled state or validation reason.

Expected Assertions:
- Ineligible wallet is hidden, disabled, or shows eligibility reason.
- Submit is unavailable.
- No transaction reference appears.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Source dropdown option | option by wallet visible name | fragile | `TransactionForm.tsx` |
| Disabled submit | disabled submit button | acceptable | `TransactionForm.tsx` |

Page Object Suggestions:
- `TransactionFormPage.expectIneligibleWalletUnavailable(walletName)`

Async/Wait Rules:
- Wait for account dropdown options.

Mutation Risk:
- none

Automation Readiness:
- blocked

Data-testid Recommendations:
- `transaction-account-option-disabled`
- `transaction-account-option-reason`

Open Questions:
- Current source has no archived/ineligible wallet state or eligibility reason in account model.

### TC-UI-OPS-LIFE-003 - Submit a burn transaction

Source requirement:
- `OPS-LIFE-003 — Submit a burn transaction`

Type:
- workflow

Role:
- Operations user.

Routes:
- `/transactions/new`
- `/queue`
- `/transactions/history`

Preconditions:
- Digital source wallet exists with sufficient balance.
- Fiat destination account exists.

Test Data:
- source wallet: digital asset account.
- destination account: fiat account.
- amount: QA-safe amount.

UI Operation Steps:
1. Open `/transactions/new`.
2. Select digital source wallet.
3. Select fiat destination account.
4. Fill amount and memo.
5. Submit.
6. Verify pending queue/history values.
7. Approve and poll completion if integrated.

Expected Assertions:
- Transaction appears with digital source and fiat destination.
- Status eventually reaches completed/successful in integrated environment.
- Balance/release outcome is asserted only if exposed by UI.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Source dropdown | source account dropdown button | fragile | `TransactionForm.tsx` |
| Destination dropdown | destination account dropdown button | fragile | `TransactionForm.tsx` |
| History status | row-scoped status badge | acceptable | `TransactionHistory.tsx` |

Page Object Suggestions:
- `TransactionFormPage.submitDigitalToFiatTransaction(data)`

Async/Wait Rules:
- Poll history for completed status after approval.

Mutation Risk:
- creates backend/UI transaction

Automation Readiness:
- needs-live-verification

Data-testid Recommendations:
- `history-status-badge`
- `history-transaction-row`

Open Questions:
- Confirm whether burn is inferred from digital-to-fiat account selection or requires a separate transaction type control not present in source.

### TC-UI-OPS-WAL-001 - Submit a new wallet request

Source requirement:
- `OPS-WAL-001 — Submit a new wallet request`

Type:
- workflow

Role:
- Wallet-management user.

Routes:
- TBD

Preconditions:
- Wallet-management UI exists.

Test Data:
- account, asset, fiat-ledger linkage.

UI Operation Steps:
1. TBD after wallet-management route/component exists.

Expected Assertions:
- Required wallet request fields visible.
- Request reference/status visible after submit.
- Wallet not displayed active before creation succeeds.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Wallet route | TBD | fragile | not found |

Page Object Suggestions:
- `WalletManagementPage.submitNewWalletRequest(data)`

Async/Wait Rules:
- Poll request status once UI exists.

Mutation Risk:
- creates wallet request

Automation Readiness:
- blocked

Data-testid Recommendations:
- `wallet-request-form`
- `wallet-request-submit`
- `wallet-request-status`

Open Questions:
- No wallet-management route or component found in current source.

### TC-UI-OPS-WAL-002 - Display wallet creation status

Source requirement:
- `OPS-WAL-002 — Display wallet creation status`

Type:
- functional

Role:
- Wallet-management user.

Routes:
- TBD

Preconditions:
- Wallet-creation status UI exists.

Test Data:
- submitted wallet request.

UI Operation Steps:
1. TBD after wallet status UI exists.

Expected Assertions:
- Created wallet identifier/status visible.
- Failed wallet creation not shown as active.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Wallet status | TBD | fragile | not found |

Page Object Suggestions:
- `WalletManagementPage.expectWalletCreationStatus(...)`

Async/Wait Rules:
- Poll wallet creation status once endpoint/UI exists.

Mutation Risk:
- none

Automation Readiness:
- blocked

Data-testid Recommendations:
- `wallet-status-row`
- `wallet-status-badge`

Open Questions:
- No wallet creation status UI found in current source.

### TC-UI-OPS-WAL-003 - Submit an existing wallet modification

Source requirement:
- `OPS-WAL-003 — Submit an existing wallet modification`

Type:
- workflow

Role:
- Wallet-management user.

Routes:
- TBD

Preconditions:
- Wallet maintenance UI exists.

Test Data:
- active wallet eligible for maintenance.

UI Operation Steps:
1. TBD after wallet modification UI exists.

Expected Assertions:
- Modification form visible.
- Request status visible.
- Updated wallet details visible after success.
- Failed modification preserves previous details.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Wallet modification form | TBD | fragile | not found |

Page Object Suggestions:
- `WalletManagementPage.submitWalletModification(data)`

Async/Wait Rules:
- Poll modification status once UI exists.

Mutation Risk:
- updates wallet

Automation Readiness:
- blocked

Data-testid Recommendations:
- `wallet-modification-form`
- `wallet-modification-submit`

Open Questions:
- No wallet modification UI found in current source.

### TC-UI-OPS-HIST-001 - View transaction status by reference

Source requirement:
- `OPS-HIST-001 — View transaction status by reference`

Type:
- functional

Role:
- Operations user.

Routes:
- `/transactions/history`

Preconditions:
- Transaction exists and user has view permission.

Test Data:
- known transaction id/reference.

UI Operation Steps:
1. Open `/transactions/history`.
2. Locate transaction row by reference. Current source has no dedicated transaction-id search input, so row lookup may require table text filtering in Playwright.
3. Click row to open transaction modal.
4. Verify row and modal details.

Expected Assertions:
- Matching row is displayed.
- Row shows transaction id, from, to, amount, and status.
- Modal shows transaction id, flow title, account nodes, and processing pipeline when applicable.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| History heading | `getByRole('heading', { name: 'Transaction Log' })` | stable | `TransactionHistory.tsx` |
| Transaction row | `getByRole('row').filter({ hasText: reference })` | acceptable | `TransactionHistory.tsx` |
| Transaction modal id | `getByText(new RegExp('ID: ' + reference))` | acceptable | `TransactionModal.tsx` |

Page Object Suggestions:
- `HistoryPage.goto()`
- `HistoryPage.openTransaction(reference)`
- `TransactionModal.expectDetails(criteria)`

Async/Wait Rules:
- Wait for history rows to render.

Mutation Risk:
- none

Automation Readiness:
- needs-live-verification

Data-testid Recommendations:
- `history-search-transaction-id`
- `history-transaction-row`
- `transaction-modal`

Open Questions:
- Current source lacks a transaction-reference search field; should one be added or should automation use row text?

### TC-UI-OPS-HIST-002 - Filter transaction history

Source requirement:
- `OPS-HIST-002 — Filter transaction history`

Type:
- functional

Role:
- Operations user.

Routes:
- `/transactions/history`

Preconditions:
- History contains multiple transactions with different senders, receivers, amounts, and statuses.

Test Data:
- sender filter text.
- receiver filter text.
- min/max amount.
- status value.

UI Operation Steps:
1. Open `/transactions/history`.
2. Click `From` header search icon.
3. Fill `Filter Sender...`.
4. Verify rows match sender filter.
5. Click `To` header search icon.
6. Fill `Filter Receiver...`.
7. Verify rows match combined filters.
8. Click `Amount` header search icon.
9. Fill `Min` and `Max`.
10. Verify amount range.
11. Click `Status` header search icon.
12. Select target status.
13. Verify status values.
14. Click `Clear All`.
15. Verify filters collapse and unfiltered set returns.

Expected Assertions:
- Every displayed row matches active filters.
- Non-matching rows are not displayed.
- `Clear All` hides filter row. Note: current source clears filter UI visibility but does not reset filter state values; this should be verified and may be a product bug.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| From filter trigger | header containing `From` then click search icon area | fragile | `TransactionHistory.tsx` |
| Sender filter | `getByPlaceholder('Filter Sender...')` | acceptable | `TransactionHistory.tsx` |
| Receiver filter | `getByPlaceholder('Filter Receiver...')` | acceptable | `TransactionHistory.tsx` |
| Min amount | `getByPlaceholder('Min')` | acceptable | `TransactionHistory.tsx` |
| Max amount | `getByPlaceholder('Max')` | acceptable | `TransactionHistory.tsx` |
| Status dropdown | `locator('select')` scoped to status filter row | fragile | `TransactionHistory.tsx` |
| Clear All | `getByRole('button', { name: /Clear All/i })` | stable | `TransactionHistory.tsx` |

Page Object Suggestions:
- `HistoryPage.applySenderFilter(value)`
- `HistoryPage.applyReceiverFilter(value)`
- `HistoryPage.applyAmountRange(min, max)`
- `HistoryPage.applyStatusFilter(status)`
- `HistoryPage.expectRowsMatchFilters(criteria)`

Async/Wait Rules:
- Filtering is client-side; wait for DOM update after input/select changes.

Mutation Risk:
- none

Automation Readiness:
- ready

Data-testid Recommendations:
- `history-filter-from-trigger`
- `history-filter-to-trigger`
- `history-filter-amount-trigger`
- `history-filter-status-trigger`
- `history-clear-filters`
- `history-transaction-row`

Open Questions:
- Should `Clear All` reset filter values or only hide controls? Current source only hides controls.

### TC-UI-OPS-HIST-003 - Display an unavailable transaction outcome

Source requirement:
- `OPS-HIST-003 — Display an unavailable transaction outcome`

Type:
- functional

Role:
- Operations user.

Routes:
- `/transactions/history`

Preconditions:
- History service can return unavailable/error state.

Test Data:
- `service-unavailable` outcome.

UI Operation Steps:
1. Configure history service to return unavailable.
2. Open `/transactions/history`.
3. Observe error state and retry control.

Expected Assertions:
- Operator-readable retrieval error is visible.
- Retry control is visible.
- Stale rows are not shown as current without explicit stale indication.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Error state | TBD | fragile | not found |
| Retry action | TBD | fragile | not found |

Page Object Suggestions:
- `HistoryPage.expectServiceUnavailable()`

Async/Wait Rules:
- Wait for failed service response state.

Mutation Risk:
- none

Automation Readiness:
- blocked

Data-testid Recommendations:
- `history-error-message`
- `history-retry-button`
- `history-stale-data-banner`

Open Questions:
- Current source keeps transactions in local/app state and has no explicit history service error UI.

### TC-UI-OPS-HIST-004 - Download filtered transactions

Source requirement:
- `OPS-HIST-004 — Download filtered transactions`

Type:
- functional

Role:
- Operations user.

Routes:
- `/transactions/history`

Preconditions:
- History contains filterable records.
- Export implementation is connected to browser download.

Test Data:
- filter criteria for successful transactions.

UI Operation Steps:
1. Open `/transactions/history`.
2. Apply filters.
3. Click `Export Data`.
4. Wait for browser download event.
5. Parse downloaded file.

Expected Assertions:
- Download event fires.
- File name/type matches product contract.
- Export contains only filtered records.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Export button | `getByRole('button', { name: 'Export Data' })` | stable | `TransactionHistory.tsx` |

Page Object Suggestions:
- `HistoryPage.exportData()`

Async/Wait Rules:
- Use Playwright download event.

Mutation Risk:
- downloads file

Automation Readiness:
- needs-live-verification

Data-testid Recommendations:
- `history-export-button`

Open Questions:
- Current source shows `Export Data` button but no click handler was found; confirm implementation or add export handler.

### TC-UI-OPS-OBS-001 - Display service health or unavailable state

Source requirement:
- `OPS-OBS-001 — Display service health or unavailable state`

Type:
- functional

Role:
- Operations user.

Routes:
- TBD

Preconditions:
- Operational health view exists.

Test Data:
- health state: healthy/unavailable.

UI Operation Steps:
1. TBD after health route/component exists.

Expected Assertions:
- Service health state visible.
- Relevant service names visible.
- Unavailable service not shown as healthy.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Health view | TBD | fragile | not found |

Page Object Suggestions:
- `HealthPage.expectServiceHealth(...)`

Async/Wait Rules:
- Poll or wait for health endpoint state once UI exists.

Mutation Risk:
- none

Automation Readiness:
- blocked

Data-testid Recommendations:
- `health-service-row`
- `health-status-badge`

Open Questions:
- No health route or operational-health component found in current source.

### TC-UI-OPS-OBS-002 - Recover a processing transaction after delayed webhook

Source requirement:
- `OPS-OBS-002 — Recover a processing transaction after delayed webhook`

Type:
- workflow

Role:
- Operations user.

Routes:
- `/transactions/history`
- `/dashboard`

Preconditions:
- Transaction can be held in processing state and later updated by polling/webhook.
- App polling updates `processingStatus`.

Test Data:
- transaction with delayed webhook outcome.

UI Operation Steps:
1. Create or seed transaction in processing state.
2. Open `/transactions/history` or `/dashboard`.
3. Verify processing status / processing badge.
4. Trigger or wait for webhook outcome.
5. Refresh/poll UI.
6. Verify same transaction reaches successful/completed status.
7. Verify no duplicate row is created.

Expected Assertions:
- Before webhook, transaction row/modal shows processing status such as `Created`, `Fiat Initiated`, `Chain Submitted`, `Settling`, or equivalent.
- After webhook, same reference/row shows `Completed`.
- Matching row count remains one for that reference.

Recommended Locators:
| UI element | Recommended locator | Stability | Source |
| --- | --- | --- | --- |
| Processing badge | `getByText(/Created|Fiat Initiated|Chain Submitted|Settling/)` | acceptable | `TransactionHistory.tsx`, `TransactionModal.tsx` |
| Completed badge | `getByText(/Completed|COMPLETED/)` | acceptable | `TransactionHistory.tsx`, `TransactionModal.tsx` |
| Transaction row | row filtered by reference | acceptable | `TransactionHistory.tsx` |

Page Object Suggestions:
- `HistoryPage.waitForProcessingStatus(reference, status)`
- `HistoryPage.expectNoDuplicateReference(reference)`

Async/Wait Rules:
- Use `expect.poll` with timeout sufficient for webhook/polling.
- Snapshot matching row count before refresh and assert no duplicate.

Mutation Risk:
- creates/updates transaction

Automation Readiness:
- needs-live-verification

Data-testid Recommendations:
- `transaction-processing-badge`
- `transaction-modal-pipeline`
- `history-transaction-row`

Open Questions:
- How can QA deterministically trigger delayed webhook outcome?
- Which route should be canonical for status refresh: dashboard, history, or modal?

## Backend API Test Cases

### TC-API-OPS-AUTH-001 - Register API user if needed and login

Source requirement:
- API smoke coverage for Orchestrator authentication reachability.

Type:
- API smoke

Role:
- API test user.

Endpoints:
- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`

Preconditions:
- API base URL is reachable.

Test Data:
- API username, email, and password from environment.

API Operation Steps:
1. Register API test user.
2. Accept successful creation or already-exists response.
3. Login with the API test user.

Expected Assertions:
- Register returns accepted/already-exists status.
- Login returns HTTP 2xx and a JSON object.

Mutation Risk:
- creates user if missing

Automation Readiness:
- ready

### TC-API-OPS-AUTH-002 - Reject login with invalid password

Source requirement:
- API functional validation for authentication failure behavior.

Type:
- API functional

Role:
- API test user.

Endpoints:
- `POST /api/v1/auth/login`

Preconditions:
- API base URL is reachable.

Test Data:
- API username from environment.
- Invalid generated password.

API Operation Steps:
1. Submit login request with valid username and invalid password.

Expected Assertions:
- Response status is 4xx.
- Response is not 5xx.

Mutation Risk:
- none

Automation Readiness:
- ready

### TC-API-OPS-TXN-001 - Reject missing transaction payload fields

Source requirement:
- API functional validation for transaction submit payload.

Type:
- API functional

Role:
- API test user.

Endpoints:
- `POST /v1/transaction`

Preconditions:
- API base URL is reachable.

Test Data:
- Empty transaction payload.
- API user id from environment.

API Operation Steps:
1. Submit transaction request with required fields missing.

Expected Assertions:
- Response status is 4xx.
- Response is not 5xx.
- No successful business transaction is created.

Mutation Risk:
- none

Automation Readiness:
- ready

### TC-API-OPS-TXN-002 - Reject malformed transaction id lookup

Source requirement:
- API functional validation for transaction status lookup.

Type:
- API functional

Role:
- API test user.

Endpoints:
- `GET /v1/transaction/{txId}`

Preconditions:
- API base URL is reachable.

Test Data:
- Malformed transaction id string.

API Operation Steps:
1. Request transaction status with a malformed id.

Expected Assertions:
- Response status is 4xx.
- Response is not 5xx.

Mutation Risk:
- none

Automation Readiness:
- ready

### TC-API-OPS-MINT-001 - Register/login, submit a MINT transaction, and confirm status

Source requirement:
- Migrated from `Da_Orchestration_E2E_Scenarios.postman_collection.json` minimal MINT flow.

Type:
- API E2E

Role:
- API test user.

Endpoints:
- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`
- `POST /v1/transaction`
- `GET /v1/transaction/{txId}`

Preconditions:
- API base URL is reachable.
- API test user can be registered or already exists.
- MINT source/destination payload is valid in the backend environment.

Test Data:
- `origin`: `fa-us-customer`
- `originJurisdiction`: `US`
- `originType`: `FIAT`
- `destination`: `5`
- `destinationJurisdiction`: `US`
- `destinationType`: `DIGITAL`
- `assetId`: `ETH_TEST5`
- `amount`: `100`

API Operation Steps:
1. Register API test user. Accept successful creation or already-exists response.
2. Login and capture token if returned.
3. Submit MINT transaction with `X-User-Id`.
4. Capture `transactionId`.
5. Poll `GET /v1/transaction/{txId}` until the configured terminal success status is reached.

Expected Assertions:
- Register returns accepted/already-exists status.
- Login succeeds.
- Submit transaction returns HTTP 2xx and a transaction id.
- Status response belongs to the submitted transaction id.
- Status response includes expected `initiatedByUserId`, `assetId`, and `amount`.
- `flowType` identifies a MINT flow.
- Final `status` is the expected terminal success status.
- `txOperations` is present as an array.
- Terminal failure statuses fail immediately.

Client / Data Suggestions:
- `api/AuthApiClient.ts`
- `api/TransactionApiClient.ts`
- `data/api-scenarios/mint-scenarios.ts`

Async/Wait Rules:
- Use polling for transaction completion.
- Playwright test timeout must be longer than transaction polling timeout.

Mutation Risk:
- creates backend transaction

Automation Readiness:
- ready

## Cross-Cutting Data-testid Recommendations

- `login-username`
- `login-password`
- `login-submit`
- `nav-dashboard`
- `nav-new-transaction`
- `nav-queue`
- `nav-history`
- `nav-liquidity`
- `transaction-form`
- `transaction-source-account-dropdown`
- `transaction-source-account-option`
- `transaction-destination-account-dropdown`
- `transaction-destination-account-option`
- `transaction-amount-input`
- `transaction-memo-input`
- `transaction-submit-button`
- `transaction-validation-message`
- `transaction-success-banner`
- `approval-queue-table`
- `approval-queue-row`
- `approval-approve-button`
- `approval-reject-button`
- `history-table`
- `history-transaction-row`
- `history-status-badge`
- `history-filter-from-trigger`
- `history-filter-to-trigger`
- `history-filter-amount-trigger`
- `history-filter-status-trigger`
- `history-export-button`
- `transaction-modal`
- `transaction-modal-close`
- `transaction-modal-pipeline`

## Global Open Questions

- Confirm live route/UI differences, especially network dropdown and explicit transaction type controls seen in live UI but not present in current source.
- Confirm canonical seeded users for admin, initiator, approver, viewer, and no-entitlement cases.
- Confirm whether Queue and History use different account display names/IDs in integrated environment.
- Confirm deterministic service controls for `mint-reserve-rejected`, `mint-fireblocks-failed`, `mint-webhook-delayed`, `transfer-success`, `burn-success`, and `service-unavailable`.
- Confirm whether wallet-management and operational-health views are planned but not yet present in current UI source.
- Confirm whether `Export Data` has an implementation behind the visible button.
