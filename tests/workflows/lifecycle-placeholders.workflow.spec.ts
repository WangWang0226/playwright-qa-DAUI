import { test } from '@playwright/test';

test.skip('TC-UI-OPS-LIFE-002 prevent transfer from an ineligible wallet requires archived/ineligible wallet fixture', async () => {
  // Needs a seeded ineligible wallet and expected disabled/error UI.
});
